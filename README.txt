Added line breaks, indentation so that all opening and closing tags are on the same level
Changed "font" to "font-color"
Removed HTML comment inside of javascript block
Checked that images have alt attributes
Created folder structure for css stylesheets
Encountered issue where certain images stop rendering when stylesheets are included
Resolved image rendering issue by removing / before filepath reference
Located more folders with necessary content in inspector menu, created folder structure to match website -> fixed menu and image issues
